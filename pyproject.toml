[project]
name = "oneopes-original"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shayestebazsefidpar-cyber", email = "shayestebazsefidpar@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "marimo>=0.24.0",
]

[project.scripts]
oneopes-original = "oneopes_original:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
